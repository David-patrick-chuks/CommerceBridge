import { Router } from 'express';

const router = Router();

// Placeholder routes - will be implemented later
router.get('/status', (req, res) => {
  res.json({ message: 'Orders routes placeholder' });
});

export default router; 


